<h1>🛒 Carrito</h1>

{{#if products.length}}
  <ul>
    {{#each products}}
      <li>
        <strong>{{this.product.name}}</strong><br>
        Precio: ${{this.product.price}}<br>
        Descripción: {{this.product.description}}<br>
        Cantidad: {{this.quantity}}<br>
        <img src="{{this.product.thumbnails.[0]}}" width="60" alt="imagen">
        <hr>
      </li>
    {{/each}}
  </ul>
{{else}}
  <p>Este carrito está vacío 💤</p>
{{/if}}
